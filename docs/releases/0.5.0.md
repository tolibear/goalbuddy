# GoalBuddy 0.5.0: Integrated Goal Compiler

GoalBuddy 0.5.0 makes the local GoalBuddy fork one coherent installed product. Codex Goal Compiler is now the user-facing route selector, Goal Prep is its explicit GoalBuddy backend, and GoalBuddy itself owns both skill payloads and the runtime contract between them.

## What changes

- `codex-goal-compiler/` and `goalbuddy/` are canonical sibling skill trees in this repository and byte-exact plugin mirrors.
- Codex and Claude installs receive both skills. The plugin composer opens Codex Goal Compiler; direct Goal Prep remains available for intentional board intake or repair.
- `goalbuddy contract --target codex|claude --json` exposes compiler contract v1: product and board-schema versions, named runtime capabilities, source provenance, and target readiness.
- The compiler preflight consumes only that contract. It requires its seven safety capabilities but accepts additive capabilities and fields, so ordinary GoalBuddy evolution no longer breaks on an exact version, agent count, file list, or doctor-output shape.
- Doctors fail closed when the bundled compiler or backend is missing or stale. Codex also rejects a second standalone compiler that could shadow the product-owned copy.
- This fork is a private local package tied to `Danielalnajjar/goalbuddy`; npm publishing machinery is removed so it cannot overwrite the upstream package.

## What does not change

- Board schema remains `version: 2`.
- Checker, receipt, Keeper, Ledger, prompt, resume, transition, and execution semantics are unchanged by this ownership cutover.
- Existing and historical boards are not migrated or rewritten.
- The compiler keeps its own audited semantic version (`3.2.1`) inside the GoalBuddy product release.
- Existing inherited `goal-maker` aliases remain untouched in this release. No new compatibility layer is introduced.

## Activation boundary

The candidate is built and tested in an isolated worktree and isolated Codex/Claude homes. Making it live is a separate owner-approved step:

1. Land the GoalBuddy candidate and the skills-repo ownership-transfer commit.
2. Point Bun's existing local GoalBuddy dependency at the landed checkout.
3. Refresh Codex and Claude from the GoalBuddy installer.
4. Remove the old standalone Codex Goal Compiler link only after the bundled compiler is proven present.
5. Run both compiler contracts and both doctors.
6. Verify representative current boards are byte-identical and still resume/prompt correctly.

If any activation proof fails, preserve the current 0.4.0 installation and boards; do not introduce a runtime shim or rewrite board history.
